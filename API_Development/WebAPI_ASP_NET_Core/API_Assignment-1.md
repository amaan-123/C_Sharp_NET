## Assignment: Complete the Books API

### Requirements

#### Complete the CRUD operations:

- Implement PUT /api/books/{id} to update an existing book
- Implement DELETE /api/books/{id} to remove a book
- Add GET /api/books/search?genre={genre} to filter books by genre


#### Add validation:
-- Ensure all data annotations work properly
-- Return appropriate HTTP status codes for different scenarios
--- Handle edge cases (empty lists, invalid IDs, etc.)

#### Testing requirements:
- Test all endpoints using Swagger UI
- Show validation working with invalid data